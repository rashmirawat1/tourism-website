{
  "name": "tourism-website",
  "version": "1.0.0",
  "description": "A simple static tourism website",
  "private": true,
  "scripts": {
    "test": "npm run validate:html && npm run check:links"
  },
  "devDependencies": {
    "html-validator": "^7.0.0",
    "broken-link-checker": "^0.7.8"
  }
}
